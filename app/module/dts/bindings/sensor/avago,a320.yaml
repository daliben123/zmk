# 路径：/__w/bbq20/bbq20/zmk/app/module/dts/bindings/sensor/avago,a320.yaml

description: Avago A320 Motion Sensor
compatible: "avago,a320"
include: [sensor.yaml]

properties:
  # 新增以下GPIO属性声明
  nrst-gpios:
    type: phandle-array
    required: false
    description: Hardware reset GPIO (active-low)
  
  shutdown-gpios:
    type: phandle-array
    required: false
    description: Power control GPIO (active-high)
  
  motion-gpios:
    type: phandle-array
    required: false
    description: Motion detection interrupt GPIO
